<?php
declare(strict_types=1);
require __DIR__ . '/config.php'; require __DIR__ . '/functions.php';
if(logged_in()){header('Location: dashboard');exit;}$flash=pull_flash();$error='';
if($_SERVER['REQUEST_METHOD']==='POST'){verify_csrf();$email=strtolower(trim((string)($_POST['email']??'')));$stmt=$pdo->prepare('SELECT * FROM users WHERE email=?');$stmt->execute([$email]);$user=$stmt->fetch();if($user&&password_verify((string)($_POST['password']??''),$user['password'])){session_regenerate_id(true);$_SESSION['user']=['id'=>(int)$user['id'],'name'=>$user['name'],'email'=>$user['email'],'role'=>$user['role']];header('Location: dashboard');exit;}$error='Email atau password tidak sesuai.';}
?>
<!doctype html><html lang="id"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Masuk — Twibbo</title><link rel="stylesheet" href="style.css"><link rel="stylesheet" href="teal.css"><link rel="stylesheet" href="auth.css"></head><body><main class="auth-page"><section class="auth-visual"><a class="brand" href="home.php"><span class="brand-mark">T</span>Twibbo by Apins Digital</a><div class="auth-copy"><span class="eyebrow">Selamat datang kembali</span><h1>Satu tautan untuk menggerakkan banyak orang.</h1><p>Masuk untuk membuat, mengelola, dan membagikan template twibbon milikmu.</p></div><small>© <?=date('Y')?> Apins Digital</small></section><section class="auth-form-wrap"><div class="auth-card"><h2>Masuk</h2><p class="subtitle">Lanjutkan mengelola kampanyemu.</p><?php if($flash):?><div class="alert alert-<?=e($flash['type'])?>" style="margin-top:20px"><?=e($flash['message'])?></div><?php endif?><?php if($error):?><div class="alert alert-error" style="margin-top:20px"><?=e($error)?></div><?php endif?><form method="post"><input type="hidden" name="csrf_token" value="<?=e(csrf_token())?>"><div class="field"><label for="email">Email</label><input id="email" name="email" type="email" required autocomplete="email" autofocus></div><div class="field"><label for="password">Password</label><input id="password" name="password" type="password" required autocomplete="current-password"></div><button class="btn btn-primary" style="width:100%">Masuk ke dashboard</button></form><p class="auth-switch">Belum punya akun? <a href="register.php">Daftar gratis</a></p></div></section></main></body></html>
