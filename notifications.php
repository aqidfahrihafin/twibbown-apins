<?php
declare(strict_types=1);require __DIR__.'/config.php';require __DIR__.'/functions.php';require_login();$user=current_user();if($_SERVER['REQUEST_METHOD']==='POST'){verify_csrf();$pdo->prepare('UPDATE notifications SET is_read=1 WHERE user_id=?')->execute([$user['id']]);redirect('notifications');}$stmt=$pdo->prepare('SELECT * FROM notifications WHERE user_id=? ORDER BY created_at DESC LIMIT 50');$stmt->execute([$user['id']]);$items=$stmt->fetchAll();
?><!doctype html><html lang="id"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Notifikasi — Semarakin</title><link rel="stylesheet" href="style.css"><link rel="stylesheet" href="teal.css"><link rel="stylesheet" href="auth.css"></head><body><header class="site-header"><div class="container nav"><a class="brand" href="./"><span class="brand-mark">S</span>Semarakin</a></div></header><main class="container"><div class="page-head dashboard-head"><div><span class="eyebrow">Pusat kabar</span><h1 class="page-title">Notifikasi</h1><p class="subtitle">Pembaruan karya dan aktivitas akunmu.</p></div><?php if($items):?><form method="post"><input type="hidden" name="csrf_token" value="<?=e(csrf_token())?>"><button class="btn btn-secondary">Tandai semua dibaca</button></form><?php endif?></div><div class="notification-list"><?php foreach($items as $item):?><a class="notification-item <?=$item['is_read']?'':'unread'?>" href="<?=e($item['url']?:'dashboard')?>"><span>✦</span><div><strong><?=e($item['message'])?></strong><small><?=date('d M Y, H:i',strtotime($item['created_at']))?></small></div></a><?php endforeach?></div><?php if(!$items):?><div class="empty">Belum ada notifikasi baru.</div><?php endif?></main><footer class="footer">Semarakin · Apins Digital</footer></body></html>
